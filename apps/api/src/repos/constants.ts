/** Repository-registration literals. */

/** Enough of the parser's stderr to say what went wrong, when the reason
 *  cannot be read out of its structured log. Not enough to ship a log file. */
export const STDERR_TAIL_LINES = 5;
